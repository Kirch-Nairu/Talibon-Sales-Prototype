import { Database, Link2, ShieldOff } from 'lucide-react';import type { MunicipalSystem } from '../../data/municipal/systems';
const tone:Record<MunicipalSystem['status'],string>={Active:'bg-emerald-50 text-emerald-800 dark:bg-emerald-950/40 dark:text-emerald-200',Integrated:'bg-blue-50 text-blue-800 dark:bg-blue-950/40 dark:text-blue-200','Not Enabled':'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-200',Planned:'bg-amber-50 text-amber-800 dark:bg-amber-950/40 dark:text-amber-200'};
export default function SystemDirectory({systems}:{systems:MunicipalSystem[]}){const groups=[...new Set(systems.map(s=>s.group))];return <div className="space-y-5">{groups.map(group=><section key={group} aria-labelledby={`system-group-${group.replaceAll(' ','-')}`}><div className="mb-2 flex items-center justify-between"><h2 id={`system-group-${group.replaceAll(' ','-')}`} className="text-sm font-bold text-slate-950 dark:text-slate-100">{group}</h2><span className="text-xs text-slate-500 dark:text-slate-400">{systems.filter(s=>s.group===group).length} systems</span></div><div className="grid gap-3 lg:grid-cols-2">{systems.filter(s=>s.group===group).map(system=><article key={system.id} className="rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-700 dark:bg-[#142236]"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="text-[10px] font-bold uppercase tracking-wide text-slate-400">{system.id}</div><h3 className="mt-1 text-base font-bold text-slate-950 dark:text-slate-100">{system.name}</h3><div className="mt-1 text-xs font-semibold text-blue-800 dark:text-blue-300">{system.owningOffice}</div></div><span className={`shrink-0 rounded-full px-2 py-1 text-[10px] font-bold ${tone[system.status]}`}>{system.status}</span></div><p className="mt-3 text-xs leading-5 text-slate-600 dark:text-slate-300">{system.purpose}</p><div className="mt-3 grid gap-2 border-t border-slate-100 pt-3 text-xs dark:border-slate-700 sm:grid-cols-2"><div className="flex gap-2 text-slate-600 dark:text-slate-300"><Database size={13} className="mt-0.5 shrink-0 text-slate-400"/><span>{system.recordScope}</span></div><div className="flex gap-2 text-slate-600 dark:text-slate-300">{system.status==='Not Enabled'?<ShieldOff size={13} className="mt-0.5 shrink-0 text-slate-400"/>:<Link2 size={13} className="mt-0.5 shrink-0 text-slate-400"/>}<span>{system.integrationNote}</span></div></div></article>)}</div></section>)}{systems.length===0&&<div className="rounded-xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500 dark:border-slate-700 dark:text-slate-400">No systems match the current filters.</div>}</div>}

import { FileText } from 'lucide-react';
import type { MunicipalMessage } from '../../data/municipal/messages';
export default function MessageHistory({messages}:{messages:MunicipalMessage[]}){return <div className="divide-y divide-slate-100 overflow-hidden rounded-xl border border-slate-200 bg-white dark:divide-slate-700 dark:border-slate-700 dark:bg-[#142236]">{messages.map(message=><article key={message.id} className="px-4 py-3.5 sm:px-5"><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-blue-50 px-2 py-0.5 text-[10px] font-bold text-blue-800 dark:bg-blue-950/40 dark:text-blue-200">{message.channel}</span>{message.priority==='High'&&<span className="text-[10px] font-bold uppercase text-rose-700 dark:text-rose-300">High priority</span>}</div><time className="text-[10px] font-semibold text-slate-400">{message.date}</time></div><h2 className="mt-2 text-sm font-bold text-slate-950 dark:text-slate-100">{message.subject}</h2><div className="mt-0.5 text-xs font-semibold text-slate-500 dark:text-slate-400">{message.office}</div><p className="mt-2 text-xs leading-5 text-slate-600 dark:text-slate-300">{message.body}</p><div className="mt-2 flex items-center gap-1 text-xs text-slate-500 dark:text-slate-400"><FileText size={12}/>{message.reference}</div></article>)}{messages.length===0&&<div className="p-8 text-center text-sm text-slate-500 dark:text-slate-400">No coordination messages match the current filters.</div>}</div>}

import { useMemo,useState } from 'react';import { MessageSquareText,Search } from 'lucide-react';import AppLayout from '../../layouts/AppLayout';import PageFrame from '../../components/PageFrame';import PageHeader from '../../components/PageHeader';import MessageHistory from '../../components/messages/MessageHistory';import {messageChannels,municipalMessages} from '../../data/municipal/messages';
export default function Index(){const[query,setQuery]=useState('');const[channel,setChannel]=useState('');const messages=useMemo(()=>{const q=query.trim().toLowerCase();return municipalMessages.filter(m=>(!channel||m.channel===channel)&&(!q||[m.subject,m.office,m.body,m.reference,m.channel].join(' ').toLowerCase().includes(q)))},[query,channel]);return <AppLayout title="Messages"><PageFrame><PageHeader eyebrow="Internal coordination" title="Messages" description="Read-only inter-office coordination history for plans, projects, meetings, documents, deadlines, and municipal operating concerns." icon={MessageSquareText} aside={<div className="rounded-xl border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-[#142236]"><div className="text-xl font-bold text-slate-950 dark:text-slate-100">{messages.length}</div><div className="text-[10px] font-bold uppercase text-slate-500">Visible messages</div></div>}/><div className="grid gap-2 rounded-xl border border-slate-200 bg-white p-3 dark:border-slate-700 dark:bg-[#142236] sm:grid-cols-[1fr_240px]"><label className="relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search coordination history" className="min-h-10 w-full rounded-lg border border-slate-300 bg-white pl-9 pr-3 text-sm dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100"/></label><select value={channel} onChange={e=>setChannel(e.target.value)} className="min-h-10 rounded-lg border border-slate-300 bg-white px-3 text-sm dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100"><option value="">All channels</option>{messageChannels.map(c=><option key={c}>{c}</option>)}</select></div><MessageHistory messages={messages}/></PageFrame></AppLayout>}

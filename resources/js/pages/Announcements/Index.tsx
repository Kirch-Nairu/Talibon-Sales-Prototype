import { useMemo,useState } from 'react';import { Bell,Search } from 'lucide-react';import AppLayout from '../../layouts/AppLayout';import PageFrame from '../../components/PageFrame';import PageHeader from '../../components/PageHeader';import AnnouncementRegister from '../../components/announcements/AnnouncementRegister';import {municipalAnnouncements} from '../../data/municipal/announcements';
export default function Index(){const[q,setQ]=useState('');const[priority,setPriority]=useState('');const items=useMemo(()=>{const needle=q.trim().toLowerCase();return municipalAnnouncements.filter(a=>(!priority||a.priority===priority)&&(!needle||[a.title,a.issuingOffice,a.audience,a.summary,a.relatedDocument].join(' ').toLowerCase().includes(needle)))},[q,priority]);return <AppLayout title="Announcements"><PageFrame><PageHeader eyebrow="Municipal notices" title="Announcements" description="Office notices and memorandum-style operational announcements showing issuing office, audience, priority, summary, and related document." icon={Bell} aside={<div className="rounded-xl border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-[#142236]"><div className="text-xl font-bold text-slate-950 dark:text-slate-100">{items.length}</div><div className="text-[10px] font-bold uppercase text-slate-500">Visible notices</div></div>}/><div className="grid gap-2 rounded-xl border border-slate-200 bg-white p-3 dark:border-slate-700 dark:bg-[#142236] sm:grid-cols-[1fr_220px]"><label className="relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search notice, office, audience, or document" className="min-h-10 w-full rounded-lg border border-slate-300 bg-white pl-9 pr-3 text-sm dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100"/></label><select value={priority} onChange={e=>setPriority(e.target.value)} className="min-h-10 rounded-lg border border-slate-300 bg-white px-3 text-sm dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100"><option value="">All priorities</option><option>Routine</option><option>Important</option><option>Urgent</option></select></div><AnnouncementRegister items={items}/></PageFrame></AppLayout>}

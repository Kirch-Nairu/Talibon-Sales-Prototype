import { useMemo,useState } from 'react';import { Network,Search } from 'lucide-react';import AppLayout from '../../layouts/AppLayout';import PageFrame from '../../components/PageFrame';import PageHeader from '../../components/PageHeader';import SystemDirectory from '../../components/municipal-systems/SystemDirectory';import {municipalSystems,systemGroups} from '../../data/municipal/systems';
export default function Index(){const[q,setQ]=useState('');const[group,setGroup]=useState('');const[status,setStatus]=useState('');const systems=useMemo(()=>{const needle=q.trim().toLowerCase();return municipalSystems.filter(s=>(!group||s.group===group)&&(!status||s.status===status)&&(!needle||[s.name,s.group,s.owningOffice,s.purpose,s.recordScope,s.integrationNote].join(' ').toLowerCase().includes(needle)))},[q,group,status]);const counts={active:systems.filter(s=>s.status==='Active').length,integrated:systems.filter(s=>s.status==='Integrated').length,notEnabled:systems.filter(s=>s.status==='Not Enabled').length,planned:systems.filter(s=>s.status==='Planned').length};return <AppLayout title="Municipal Systems"><PageFrame><PageHeader eyebrow="Municipal systems map" title="Municipal Systems" description="Internal directory of municipal systems, their owning offices, operating status, record scope, and current integration context." icon={Network}/><section className="grid grid-cols-2 gap-2 sm:grid-cols-4">{[['Active',counts.active],['Integrated',counts.integrated],['Not Enabled',counts.notEnabled],['Planned',counts.planned]].map(([label,value])=><div key={String(label)} className="rounded-xl border border-slate-200 bg-white p-3 dark:border-slate-700 dark:bg-[#142236]"><div className="text-xl font-bold text-slate-950 dark:text-slate-100">{value}</div><div className="text-xs font-semibold text-slate-500 dark:text-slate-400">{label}</div></div>)}</section><div className="grid gap-2 rounded-xl border border-slate-200 bg-white p-3 dark:border-slate-700 dark:bg-[#142236] md:grid-cols-[1fr_240px_200px]"><label className="relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search system, office, scope, or context" className="min-h-10 w-full rounded-lg border border-slate-300 bg-white pl-9 pr-3 text-sm dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100"/></label><select value={group} onChange={e=>setGroup(e.target.value)} className="min-h-10 rounded-lg border border-slate-300 bg-white px-3 text-sm dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100"><option value="">All groups</option>{systemGroups.map(g=><option key={g}>{g}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)} className="min-h-10 rounded-lg border border-slate-300 bg-white px-3 text-sm dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100"><option value="">All statuses</option><option>Active</option><option>Integrated</option><option>Not Enabled</option><option>Planned</option></select></div><SystemDirectory systems={systems}/></PageFrame></AppLayout>}

import { Link } from '@inertiajs/react';
import AppLayout from '../../layouts/AppLayout';

type RecordItem = { record_type: string; record_number: string; title: string; summary?: string; approved_at?: string; year: number; status: string; issuing_body: string; keywords?: string; creator: { name: string } };

export default function Show({ record }: { record: RecordItem; canManage: boolean }) {
    return <AppLayout title={record.record_number}><div className="mx-auto max-w-4xl space-y-6"><Link href="/legislation" className="text-sm font-semibold text-blue-700">← Back to Legislative Records</Link><article className="rounded-3xl border border-slate-200 bg-white p-7 shadow-sm md:p-10"><div className="text-xs font-bold uppercase tracking-[0.2em] text-blue-700">{record.record_type.replaceAll('_', ' ')} · {record.record_number}</div><h1 className="mt-3 text-3xl font-bold leading-tight text-slate-950">{record.title}</h1><div className="mt-5 flex flex-wrap gap-2"><span className="rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-bold uppercase text-emerald-800">{record.status}</span><span className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-bold text-slate-700">{record.year}</span></div><div className="mt-8 grid gap-5 border-y border-slate-100 py-6 sm:grid-cols-2"><div><div className="text-xs uppercase text-slate-400">Issuing body</div><div className="mt-1 font-semibold">{record.issuing_body}</div></div><div><div className="text-xs uppercase text-slate-400">Approved</div><div className="mt-1 font-semibold">{record.approved_at ? new Date(record.approved_at).toLocaleDateString() : 'Not specified'}</div></div></div><div className="mt-8"><div className="text-xs font-bold uppercase tracking-wide text-slate-400">Summary</div><p className="mt-3 whitespace-pre-wrap text-[15px] leading-7 text-slate-700">{record.summary || 'No summary recorded.'}</p></div>{record.keywords && <div className="mt-8 rounded-2xl bg-slate-50 p-4 text-sm text-slate-600"><strong>Keywords:</strong> {record.keywords}</div>}</article></div></AppLayout>;
}
